<form method="post" action="{{ path('app_admin_equipements_delete', {'id': equipement.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet équipement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipement.id) }}">
    <button class="btn  btn-danger btn-delete">Supprimer</button>
</form>
